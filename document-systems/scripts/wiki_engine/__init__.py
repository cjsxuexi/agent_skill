"""wiki_engine — deterministic parse / lint / structural-operator engine for the
document-systems + wiki-refine skills.

Python 3.8+, stdlib only. All public behavior is exercised through ``cli.py``;
agents never import internal modules. See the plan §6 and ``MAINTAINER.md`` checks
10–15 for the contract this engine enforces.
"""

__version__ = "1.0.0"
